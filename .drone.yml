pipeline:
  tests:
    image: python:3.6
    commands:
      - pip install tox
      - tox -e flake8
      - tox -e isort

  docker:
    image: plugins/docker
    when:
      branch: master
      event: push
    repo: abakus/brus
    tag: latest
